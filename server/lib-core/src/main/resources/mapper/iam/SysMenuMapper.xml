<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- IAM 菜单 SQL @author XieMingJie @since 2026-09-05 -->
<mapper namespace="cn.miyf.repository.mapper.SysMenuMapper">

    <select id="selectVisibleMenus" resultType="cn.miyf.bean.entity.SysMenuEntity">
        SELECT id, parent_id, name, path, component, icon, menu_type, permission_code,
               sort_order, visible, status, created_at, updated_at
        FROM sys_menu
WHERE visible = TRUE
          AND status = 'ENABLED'
        ORDER BY sort_order ASC, id ASC
    </select>
</mapper>
