<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- IAM 权限解析 SQL @author XieMingJie @since 2026-09-05 -->
<mapper namespace="cn.miyf.permission.repository.mapper.SysPermissionMapper">

    <sql id="PermColumns">
        id, code, name, description, group_code, perm_no, parent_id, product, tree_name, node_type, sort_order,
        create_time, last_modify_time
    </sql>

    <sql id="PermColumnsP">
        p.id, p.code, p.name, p.description, p.group_code, p.perm_no, p.parent_id, p.product, p.tree_name, p.node_type,
        p.sort_order, p.create_time, p.last_modify_time
    </sql>

    <select id="selectByCode" resultType="cn.miyf.permission.bean.entity.SysPermissionEntity">
        SELECT
        <include refid="PermColumns"/>
        FROM sys_permission
        WHERE code = #{code}
        LIMIT 1
    </select>

    <select id="selectByPermNo" resultType="cn.miyf.permission.bean.entity.SysPermissionEntity">
        SELECT
        <include refid="PermColumns"/>
        FROM sys_permission
        WHERE perm_no = #{permNo}
        LIMIT 1
    </select>

    <select id="findByUserId" resultType="cn.miyf.permission.bean.entity.SysPermissionEntity">
        SELECT DISTINCT
        <include refid="PermColumnsP"/>
        FROM sys_permission p
                 INNER JOIN sys_perm_group_item pgi ON pgi.permission_id = p.id
                 INNER JOIN sys_perm_group pg ON pg.id = pgi.group_id
                 INNER JOIN sys_role_perm_group rpg ON rpg.group_id = pg.id
                 INNER JOIN sys_role r ON r.id = rpg.role_id
                 INNER JOIN sys_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
          AND (p.node_type IS NULL OR p.node_type = 'API')
        ORDER BY p.code ASC
    </select>

    <select id="findByRoleId" resultType="cn.miyf.permission.bean.entity.SysPermissionEntity">
        SELECT DISTINCT
        <include refid="PermColumnsP"/>
        FROM sys_permission p
                 INNER JOIN sys_perm_group_item pgi ON pgi.permission_id = p.id
                 INNER JOIN sys_perm_group pg ON pg.id = pgi.group_id
                 INNER JOIN sys_role_perm_group rpg ON rpg.group_id = pg.id
        WHERE rpg.role_id = #{roleId}
          AND (p.node_type IS NULL OR p.node_type = 'API')
        ORDER BY p.code ASC
    </select>
</mapper>
