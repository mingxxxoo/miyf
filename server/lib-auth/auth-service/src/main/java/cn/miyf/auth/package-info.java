/**
 * 认证服务：登录、JWT、账号实体、权限注解（{@code @MiyfPermission} / {@code @RequirePermission}）。
 * <p>
 * 角色/权限码加载由 {@link cn.miyf.security.AdminAuthAuthorityLoader} 扩展点提供（permission-service 实现）。
 */
package cn.miyf.auth;
