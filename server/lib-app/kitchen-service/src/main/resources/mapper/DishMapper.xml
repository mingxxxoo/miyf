<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  菜品复杂查询 SQL。
  说明：联表/动态条件/原子库存/聚合一律写在本文件，禁止用 MyBatis-Plus Wrapper 拼装。
  @author XieMingJie
  @since 2026-09-04 16:35
-->
<mapper namespace="cn.miyf.kitchen.repository.mapper.DishMapper">

    <sql id="DishColumns">
        d.id, d.category_id, d.name, d.subtitle, d.description, d.cover_image,
        d.status, d.sort_order, d.is_recommend AS recommend, d.stock, d.stock_type, d.unit,
        d.rating, d.rating_count, d.created_by, d.updated_by,
        d.created_at, d.updated_at
    </sql>

    <!-- 用户端：仅展示可预约（ON_SALE） -->
    <select id="selectUserPage" resultType="cn.miyf.kitchen.bean.entity.DishEntity">
        SELECT
        <include refid="DishColumns"/>
        FROM dish d
WHERE d.status = 'ON_SALE'
          <if test="categoryId != null">
              AND d.category_id = #{categoryId}
          </if>
          <if test="recommend != null">
              AND d.is_recommend = #{recommend}
          </if>
          <if test="keyword != null and keyword != ''">
              AND (d.name ILIKE CONCAT('%', #{keyword}, '%')
                   OR d.subtitle ILIKE CONCAT('%', #{keyword}, '%'))
          </if>
        /* @conditionSql */
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countUserPage" resultType="long">
        SELECT COUNT(1)
        FROM dish d
WHERE d.status = 'ON_SALE'
          <if test="categoryId != null">
              AND d.category_id = #{categoryId}
          </if>
          <if test="recommend != null">
              AND d.is_recommend = #{recommend}
          </if>
          <if test="keyword != null and keyword != ''">
              AND (d.name ILIKE CONCAT('%', #{keyword}, '%')
                   OR d.subtitle ILIKE CONCAT('%', #{keyword}, '%'))
          </if>
    </select>

    <select id="selectAdminPage" resultType="cn.miyf.kitchen.bean.entity.DishEntity">
        SELECT
        <include refid="DishColumns"/>
        FROM dish d
          <if test="categoryId != null">
              AND d.category_id = #{categoryId}
          </if>
          <if test="status != null and status != ''">
              AND d.status = #{status}
          </if>
          <if test="keyword != null and keyword != ''">
              AND d.name ILIKE CONCAT('%', #{keyword}, '%')
          </if>
        /* @conditionSql */
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAdminPage" resultType="long">
        SELECT COUNT(1)
        FROM dish d
          <if test="categoryId != null">
              AND d.category_id = #{categoryId}
          </if>
          <if test="status != null and status != ''">
              AND d.status = #{status}
          </if>
          <if test="keyword != null and keyword != ''">
              AND d.name ILIKE CONCAT('%', #{keyword}, '%')
          </if>
    </select>

    <!-- 热门：评分优先，评价数次之；不含任何销售金额指标 -->
    <select id="selectHot" resultType="cn.miyf.kitchen.bean.entity.DishEntity">
        SELECT
        <include refid="DishColumns"/>
        FROM dish d
WHERE d.status = 'ON_SALE'
        ORDER BY d.rating DESC, d.rating_count DESC, d.updated_at DESC
        LIMIT #{limit}
    </select>

    <select id="selectRecommend" resultType="cn.miyf.kitchen.bean.entity.DishEntity">
        SELECT
        <include refid="DishColumns"/>
        FROM dish d
WHERE d.status = 'ON_SALE'
          AND d.is_recommend = TRUE
        ORDER BY d.sort_order ASC, d.created_at DESC
        LIMIT #{limit}
    </select>

    <!-- 并发安全：WHERE stock >= qty，影响行数为 0 即库存不足 -->
    <update id="deductStock">
        UPDATE dish
        SET stock = stock - #{qty},
            updated_at = NOW()
        WHERE id = #{id}
          AND stock_type = 'LIMITED'
          AND stock &gt;= #{qty}
    </update>

    <update id="restoreStock">
        UPDATE dish
        SET stock = stock + #{qty},
            updated_at = NOW()
        WHERE id = #{id}
          AND stock_type = 'LIMITED'
    </update>

    <update id="updateRating">
        UPDATE dish
        SET rating = #{rating},
            rating_count = #{ratingCount},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="countOrderItems" resultType="long">
        SELECT COUNT(1)
        FROM kitchen_order_item oi
        WHERE oi.dish_id = #{dishId}
    </select>

    <select id="countOnSale" resultType="long">
        SELECT COUNT(1)
        FROM dish d
WHERE d.status = 'ON_SALE'
    </select>

    <select id="countByCategoryId" resultType="long">
        SELECT COUNT(1)
        FROM dish d
WHERE d.category_id = #{categoryId}
    </select>
</mapper>
