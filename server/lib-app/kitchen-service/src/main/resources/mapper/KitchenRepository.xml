<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.miyf.kitchen.repository.KitchenRepository">

    <select id="selectByOwnerUserId" resultType="cn.miyf.kitchen.bean.entity.KitchenEntity">
        SELECT id, owner_user_id, name, intro, cover_image, status, create_time, last_modify_time
        FROM kitchen
        WHERE owner_user_id = #{ownerUserId}
        LIMIT 1
    </select>

    <select id="selectAdminPage" resultType="cn.miyf.kitchen.bean.entity.KitchenEntity">
        SELECT id, owner_user_id, name, intro, cover_image, status, create_time, last_modify_time
        FROM kitchen
        WHERE 1 = 1
          <if test="keyword != null and keyword != ''">
              AND name ILIKE CONCAT('%', #{keyword}, '%')
          </if>
          <if test="status != null and status != ''">
              AND status = #{status}
          </if>
        ORDER BY last_modify_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAdminPage" resultType="long">
        SELECT COUNT(1)
        FROM kitchen
        WHERE 1 = 1
          <if test="keyword != null and keyword != ''">
              AND name ILIKE CONCAT('%', #{keyword}, '%')
          </if>
          <if test="status != null and status != ''">
              AND status = #{status}
          </if>
    </select>
</mapper>
