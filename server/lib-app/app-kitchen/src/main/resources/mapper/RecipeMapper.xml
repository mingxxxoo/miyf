<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 菜谱 SQL @author XieMingJie @since 2026-09-04 16:41 -->
<!-- JSONB 字段依赖实体 @TableName(autoResultMap=true) + JacksonTypeHandler，避免 XML resultMap 冲突 -->
<mapper namespace="cn.miyf.kitchen.repository.mapper.RecipeMapper">

    <select id="selectByDishId" resultType="cn.miyf.kitchen.bean.entity.RecipeEntity">
        SELECT id, dish_id, description, difficulty, prepare_minutes, cook_minutes, servings,
               ingredients, seasonings, steps, tips, nutrition, created_at, updated_at
        FROM dish_recipe
WHERE dish_id = #{dishId}
        LIMIT 1
    </select>

    <select id="selectAll" resultType="cn.miyf.kitchen.bean.entity.RecipeEntity">
        SELECT id, dish_id, description, difficulty, prepare_minutes, cook_minutes, servings,
               ingredients, seasonings, steps, tips, nutrition, created_at, updated_at
        FROM dish_recipe
ORDER BY updated_at DESC
    </select>
</mapper>
